# Simple API

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
uvicorn app:app --reload
```

## Routes

- `/` - Hello World
- `/users` - List of users
- `/users/{user_id}` - User detail
